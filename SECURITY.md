# 安全政策

请勿在公开 issue 中贴出会话日志、密钥、令牌或个人信息。敏感问题请使用仓库 GitHub Security Advisory（如已启用），或通过仓库作者公开的联系方式私下报告。普通功能问题可附最小复现步骤与脱敏后的测试结果。

## 支持范围

当前维护版本为 2.x，面向 DSH 0.1.5。1.x 依赖旧 DSH 接口，且旧 HTTP 路由没有登录认证；请升级，不应在对外开放的服务中使用。

## 当前边界

- API 使用 DSH 官方 `connection.fetch`，沿用登录认证和 Host / Origin 检查。插件不自行保存登录凭据。
- 上传最多 256 MiB，ZIP 解压总量最多 512 MiB；所有条目在内存中解析，不按归档路径解压到文件系统。不支持 ZIP64、加密、分卷或符号链接。
- 官方格式校验、CRC 与 SHA-256 检查不证明作者身份，也不能替代可信来源。
- 会话通过官方 SessionHandle 写入，图片与文件通过官方附件服务保存。
- 撤销仅限本插件有持久记录、未打开且未改变的导入。恢复保留隔离副本，不删除共享附件。
- 多会话导入不具备官方事务支持；崩溃与恢复记录的处理见 [数据与安全边界](docs/security.md)。

请报告可复现的认证绕过、解析器越界或资源限制绕过、指纹校验绕过，以及可能处理非本次导入数据的恢复路径。
